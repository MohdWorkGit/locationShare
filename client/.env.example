# API and WebSocket Configuration
# For development or when frontend/backend are on different domains
# Leave empty to use relative URLs (recommended for Docker with nginx proxy)

# Backend API URL (without /api suffix)
# Examples:
#   https://api.yourdomain.com
#   http://localhost:5000
VITE_API_URL=

# WebSocket URL (for Socket.IO)
# Examples:
#   https://api.yourdomain.com
#   http://localhost:5000
VITE_SOCKET_URL=
