# Production Environment Configuration
# Copy this file to .env and configure for your deployment

# Frontend/Backend Communication Configuration
# For Docker deployment with nginx proxy (recommended): Leave these empty
# For separate domain deployment: Set to your backend URL

# Backend API URL (without /api suffix)
# Examples:
#   https://api.yourdomain.com
#   http://your-backend-server:5000
# Leave empty to use nginx proxy
VITE_API_URL=

# WebSocket URL (for Socket.IO)
# Examples:
#   https://api.yourdomain.com
#   http://your-backend-server:5000
# Leave empty to use nginx proxy
VITE_SOCKET_URL=

# CORS Configuration for Backend
# Comma-separated list of allowed origins
# Use '*' to allow all origins (default for Docker nginx proxy setup)
# For production with specific domains:
#   CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
# For development: http://localhost:3000,http://localhost:5173
CORS_ORIGIN=*
